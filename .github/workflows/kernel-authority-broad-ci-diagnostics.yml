name: Kernel authority broad CI diagnostics

on:
  pull_request:
    branches: [agentic-lab]
    types: [opened, synchronize, reopened]

permissions:
  contents: write

jobs:
  diagnose:
    if: github.head_ref == 'agent/kernel-authority-broad-ci-diagnostics'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out exact diagnostic branch
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          ref: agent/kernel-authority-broad-ci-diagnostics
          fetch-depth: 0
          persist-credentials: true

      - name: Set up Python
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405
        with:
          python-version: "3.11"
          cache: pip

      - name: Reproduce first broad CI failure
        id: reproduce
        shell: bash
        run: |
          set -euo pipefail
          pip install -r requirements.txt
          mkdir -p data diagnostics

          set +e
          timeout --signal=TERM --kill-after=30s 8m \
            python -m pytest -vv --tb=short --maxfail=1 --timeout=90 --timeout-method=thread \
            > diagnostics/broad-ci-full.log 2>&1
          status=$?
          set -e

          {
            echo "# Broad CI fail-fast diagnostic"
            echo
            echo "source_head_sha: 9c02b6b921b7d27febab8e3ff28f23de1c486da2"
            echo "diagnostic_head_sha: $(git rev-parse HEAD)"
            echo "exit_status: $status"
            echo
            echo "## Failure/error lines"
            grep -E '^(FAILED|ERROR) |^=+ (FAILURES|ERRORS|short test summary info|warnings summary|[0-9]+ (failed|passed|error|errors))' diagnostics/broad-ci-full.log || true
            echo
            echo "## Final 300 log lines"
            tail -n 300 diagnostics/broad-ci-full.log
          } > diagnostics/broad-ci-failure-summary.txt

          echo "status=$status" >> "$GITHUB_OUTPUT"

      - name: Commit compact diagnostic only
        run: |
          set -euo pipefail
          rm -f diagnostics/broad-ci-full.log
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add diagnostics/broad-ci-failure-summary.txt
          git commit -m 'chore(agent): record broad CI fail-fast summary'
          git push origin HEAD:agent/kernel-authority-broad-ci-diagnostics

      - name: Preserve reproduced CI status
        run: |
          test "${{ steps.reproduce.outputs.status }}" -eq 0
